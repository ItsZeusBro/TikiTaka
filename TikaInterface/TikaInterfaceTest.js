import { TikaInterface } from "./TikaInterface.js"

//This basically runs tests on every module
class TikaInterfaceTest{
    constructor(verbose){
        this.verbose = verbose
        this.ti = new TikaInterface()
    }
    
}